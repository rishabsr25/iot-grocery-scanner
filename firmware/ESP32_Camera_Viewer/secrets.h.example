#pragma once

// Copy this file to secrets.h and fill in your WiFi credentials.
// secrets.h is gitignored — do not commit real credentials.

const char* WIFI_SSID = "your-network-name";
const char* WIFI_PASSWORD = "your-network-password";

// Flask identify endpoint on your PC (plain HTTP — use WiFiClient, not WiFiClientSecure)
const char* IDENTIFY_URL = "http://192.168.1.100:5000/identify";
